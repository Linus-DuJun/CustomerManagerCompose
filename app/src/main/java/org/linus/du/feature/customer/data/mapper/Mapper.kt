package org.linus.du.feature.customer.data.mapper

// Network return XXEntry, we need map it to  XXEntity here and save the XXEntity into local DB
// Not used in this project, because we only store data in local DB